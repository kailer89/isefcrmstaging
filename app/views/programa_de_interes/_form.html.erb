<%= form_for @programa_de_intere, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', programa_de_interes_path, :class => 'btn' %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Programa De Intere</legend>

    <div class="control-group">
      <%= f.label :valor, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :valor, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', programa_de_interes_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
