<%= simple_form_for @status_del_examinado_psico, :html => { :class => 'form-horizontal' } do |f| %>
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                status_del_examinado_psicos_path, :class => 'btn' %>
  <%= f.input :valor %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                status_del_examinado_psicos_path, :class => 'btn' %>
  </div>
<% end %>
