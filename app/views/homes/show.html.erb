<h1>Home</h1>

<p>
  <b>Name</b><br>
  <%= @home.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', homes_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_home_path(@home), :class => 'btn' %>
  <%= link_to 'Delete', home_path(@home), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
