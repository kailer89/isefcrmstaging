<h1>Static</h1>

<p>
  <b>Valor</b><br>
  <%= @static.valor %>
</p>

<div class="form-actions">
  <%= link_to 'Back', statics_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_static_path(@static), :class => 'btn' %>
  <%= link_to 'Delete', static_path(@static), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
