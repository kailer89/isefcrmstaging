  <%= simple_form_for @admitido, :html => { :class => 'form-horizontal',:onsubmit=>"return confirm('Estas seguro de querer continuar?');" } do |f| %>
   <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Cancel', examinados_path,:confirm => "Estas seguro de querer continuar?", :class => 'btn' %>
    <%if request.path_parameters[:action]== "edit" %>
      <%if not @admitido.isinscrito?%>
        <%= link_to 'Convertir en Inscrito', convertir_admitido_path,:confirm => "Estas seguro de querer continuar?", :class=>'btn btn-info', :method=>'post', :id=>'convertir'%> 
      <%end%>
    <%end%>
<%=render :partial=>"shared/breadcrumbs", :locals=>{:model_name=>request.path_parameters[:controller],:model_id=>@admitido.id, :parent_model=>nil, :parent_id=>nil}%>
<%= render :partial => "admitidosNav"%>
<%=render :partial=>"shared/edit_admitidos", :locals=>{:f=>f}%>
<%=render :partial=>"shared/edit_examinados", :locals=>{:f=>f}%>

<div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Cancel', examinados_path,:confirm => "Estas seguro de querer continuar?", :class => 'btn' %>
    <%if request.path_parameters[:action]== "edit" %>
      <%if not @admitido.isinscrito?%>
        <%= link_to 'Convertir en Inscrito', convertir_admitido_path,:confirm => "Estas seguro de querer continuar?", :class=>'btn btn-info', :method=>'post', :id=>'convertir'%> 
      <%end%>
    <%end%>
</div>   
<%=render :partial=>"shared/tabed_actions", :locals=>{:prospecto_id=>@admitido.id,:real_id=>@admitido.examinados.first.solicitantes.first.prospectos.first.id,:tareas=>@tareas,:memos=>@memos,:llamadas=>@llamadas,:correos=>@correos,:attachments=>@attachments}%>
<div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Cancel', examinados_path, :class => 'btn' %>
    <%if request.path_parameters[:action]== "edit" %>
      <%if not @admitido.isinscrito?%>
        <%= link_to 'Convertir en Inscrito', convertir_admitido_path, :class=>'btn btn-info', :method=>'post', :id=>'convertir'%> 
      <%end%>
    <%end%>
</div>          
<% end %><!--End Admitido-->

<script type="text/javascript">
(function($) {
$(document).ready(function(){
  $('.datepicker').datepicker();
});
})(jQuery);

</script>