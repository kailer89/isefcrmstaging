<%=render :partial=>"shared/breadcrumbs", :locals=>{:model_name=>request.path_parameters[:controller],:model_id=>@ultimo_grado_de_estudio.id, :parent_model=>nil, :parent_id=>nil}%>
<%= form_for @ultimo_grado_de_estudio, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', ultimo_grado_de_estudios_path, :class => 'btn' %>
  <fieldset>
<div class="well">    <legend><%= controller.action_name.capitalize %> Ultimo Grado De Estudio</legend>

    <div class="control-group">
      <%= f.label :grado_de_estudio, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :grado_de_estudio, :class => 'text_field' %>
      </div>
    </div>
    </div>
<%=render :partial=>"shared/tareas", :locals=>{:model_id=>@ultimo_grado_de_estudio.id}%>
<%=render :partial=>"shared/memos", :locals=>{:model_id=>@ultimo_grado_de_estudio.id}%>
<%=render :partial=>"shared/attachments", :locals=>{:model_id=>@ultimo_grado_de_estudio.id}%>
<%=render :partial=>"shared/llamadas", :locals=>{:model_id=>@ultimo_grado_de_estudio.id}%>
    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', ultimo_grado_de_estudios_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
