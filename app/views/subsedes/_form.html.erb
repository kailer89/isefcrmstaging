<%= simple_form_for @subsede, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', subsedes_path, :class => 'btn' %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Extensión</legend>

    <%=f.input :nombre%>
    <%=f.input :descripcion, :input_html=>{:cols=>50, :rows=>3}%>
    <%=f.association :sede,:include_blank => false, :label_method => :nombre %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', subsedes_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
