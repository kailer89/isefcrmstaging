<h1>Purge</h1>

<p>
  <b>Name</b><br>
  <%= @purge.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', purges_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_purge_path(@purge), :class => 'btn' %>
  <%= link_to 'Delete', purge_path(@purge), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
