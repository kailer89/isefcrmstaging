<h1>Sede</h1>

<p>
  <b>Nombre</b><br>
  <%= @sede.nombre %>
</p>

<div class="form-actions">
  <%= link_to 'Back', sedes_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_sede_path(@sede), :class => 'btn' %>
  <%= link_to 'Delete', sede_path(@sede), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
