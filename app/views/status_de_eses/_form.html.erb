<%= form_for @status_de_ese, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', status_de_eses_path, :class => 'btn' %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Status De Ese</legend>

    <div class="control-group">
      <%= f.label :valor, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :valor, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', status_de_eses_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
