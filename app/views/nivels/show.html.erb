<h1>Nivel</h1>

<p>
  <b>Valor</b><br>
  <%= @nivel.valor %>
</p>

<div class="form-actions">
  <%= link_to 'Back', nivels_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_nivel_path(@nivel), :class => 'btn' %>
  <%= link_to 'Delete', nivel_path(@nivel), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
