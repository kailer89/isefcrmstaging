<%= form_for @periodo_para_ingresar, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', periodo_para_ingresars_path, :class => 'btn' %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Periodo Para Ingresar</legend>

    <div class="control-group">
      <%= f.label :valor, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :valor, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', periodo_para_ingresars_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
