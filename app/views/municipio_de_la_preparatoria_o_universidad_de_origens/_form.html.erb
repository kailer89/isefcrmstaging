<%= form_for @municipio_de_la_preparatoria_o_universidad_de_origen, :html => { :class => 'form-horizontal' } do |f| %>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', municipio_de_la_preparatoria_o_universidad_de_origens_path, :class => 'btn' %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Municipio De La Preparatoria O Universidad De Origen</legend>

    <div class="control-group">
      <%= f.label :valor, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :valor, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', municipio_de_la_preparatoria_o_universidad_de_origens_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
